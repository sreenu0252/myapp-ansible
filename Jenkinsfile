pipeline{
  agent any
     stages{
       stage('scm checkout'){
         step{
           git 'https://github.com/sreenu0252/myapp-ansible.git'
         }
      }
       stage('ansible-playbook'){
         step{
           
         }
      }
  }
}
